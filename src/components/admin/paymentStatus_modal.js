import React from 'react';
import styled from 'styled-components';

const Modal = (props) => {
  const { isActive, modalType, setModalIsActive } = props;
  console.log('modal', isActive, modalType);
  return <div></div>;
};

export default Modal;
